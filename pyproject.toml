[project]
name = "django-coupons"
version = "1.3.0"
description = "A reuseable Django application for coupon gereration and handling"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "django>=4.2.0",
]

[dependency-groups]
dev = [
    "coverage>=7.6.1",
    "packaging>=25.0",
]
